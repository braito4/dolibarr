Evidence for the zero-price display correction split from Dolibarr/dolibarr#40577.

The image renders the actual affected PHP view blocks using isolated fixtures, simplified CSS and label helpers. It is not a screenshot of a running Dolibarr installation. The local MySQL demo was unavailable during this split.
